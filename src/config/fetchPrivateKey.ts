/**
 * LOCAL DEV STUB. Only imported by server.ts — its returned value isn't
 * actually used anywhere in the portion of server.ts that runs (destructured
 * but never called in the code present in this checkout).
 */
export async function fetchPrivateKey(): Promise<string | null> {
  return null;
}
