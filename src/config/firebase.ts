/**
 * LOCAL DEV STUB. server.ts imports this for its side effect (Firebase Admin
 * SDK init, presumably for FCM push notifications) but never uses the
 * imported binding directly — safe to no-op for local boot.
 */
export {};
