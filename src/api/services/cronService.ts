/** LOCAL DEV STUB — no-op. */
class CronService {
  initializeCronJob(): void {
    console.log("[stub] cronService.initializeCronJob — no-op, real service not present in this checkout");
  }
}

export default new CronService();
